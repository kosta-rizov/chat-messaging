import Button from "@/components/ui/Button";

export default function Home() {
  return (
    <Button 
      variant={"default"}
      size={"lg"}
      isLoading={false}
      >
      Send Message
    </Button>
  );
}

